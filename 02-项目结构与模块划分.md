# 第2章：项目结构与模块划分

## 2.1 目录结构详解

Claude Code 采用扁平化的项目结构，所有源代码集中在 `src` 目录下。下表展示了项目的顶层目录结构：

```
claude-code/
  src/
    ├── assistant/           # 助手功能模块
    ├── bootstrap/           # 应用启动与状态管理
    ├── bridge/             # 远程桥接与会话管理
    ├── buddy/               # 伴侣精灵（UI增强）
    ├── cli/                # 命令行接口层
    ├── commands/           # CLI 命令实现
    ├── components/         # React UI 组件库
    ├── constants/          # 常量与配置定义
    ├── context/            # React Context 上下文
    ├── coordinator/         # 协调器模式（多Agent协调）
    ├── entrypoints/         # 应用入口点
    ├── hooks/              # 自定义 React Hooks
    ├── ink/               # 终端渲染引擎（基于 Ink 框架）
    ├── keybindings/         # 快捷键绑定系统
    ├── memdir/             # 内存/记忆存储
    ├── migrations/          # 数据迁移脚本
    ├── native-ts/           # 原生 TypeScript 模块
    ├── outputStyles/         # 输出样式配置
    ├── plugins/             # 插件系统
    ├── query/              # 查询引擎
    ├── remote/             # 远程会话管理
    ├── schemas/             # JSON Schema 定义
    ├── screens/              # 屏幕组件
    ├── server/             # 服务器功能
    ├── services/            # 核心业务服务
    ├── skills/             # 技能系统
    ├── state/             # 全局状态管理
    ├── tasks/              # 任务执行模块
    ├── tools/             # 工具实现
    ├── types/              # TypeScript 类型定义
    ├── upstreamproxy/       # 上游代理
    ├── utils/              # 工具函数库
    ├── vim/                # Vim 模式支持
    └── voice/              # 语音功能
```

### 根心文件概览

| 文件 | 大小 | 职责说明 |
|------|------|-------------|
| `main.tsx` | 808KB | 主入口，启动 REPL 应用 |
| `QueryEngine.ts` | 48KB | 查询引擎 |
| `Tool.ts` | 30KB | 工具基类 |
| `commands.ts` | 26KB | 命令注册表 |
| `query.ts` | 70KB | 查询功能 |
| `interactiveHelpers.tsx` | 58KB | 交互式 UI 辅助函数 |
| `history.ts` | 23KB | 历史记录管理 |
| `setup.ts` | 21KB | 应用初始化 |
| `context.ts` | 6.6KB | 上下文管理 |
| `ink.ts` | 4KB | Ink 渲染导出 |
| `cost-tracker.ts` | 11KB | 成本追踪 |
| `tools.ts` | 18KB | 工具集合 |

## 2.2 核心模块职责

### 2.2.1 入口与启动模块 (entrypoints/)

**职责**：应用程序入口点，处理命令行参数解析和初始化流程。

**核心文件**：
- `cli.tsx`：CLI 入口，处理快速路径（如 `--version`）
- `init.ts`：初始化逻辑，- `mcp.ts`：MCP 服务器入口
- `agentSdkTypes.ts`：Agent SDK 类型定义

### 2.2.2 状态管理模块 (bootstrap/, state/)

**职责**：管理全局应用状态，提供跨组件状态共享机制。

**核心文件**：
- `bootstrap/state.ts`：全局状态定义和访问器
- `state/AppState.tsx`：React 状态组件
- `state/AppStateStore.ts`：状态存储定义
- `state/store.ts`：状态存储创建

### 2.2.3 命令系统 (commands/)

**职责**：实现各种 CLI 子命令，如 `/commit`、`/review`、`/help` 等。

**特点**：
- 采用模块化设计，每个命令独立目录
- 支持命令自动注册和发现
- 包含 70+ 个子命令

**主要命令目录**：
- `add-dir/`：添加目录
- `commit/`：Git 提交
- `review/`：PR 宣查
- `mcp/`：MCP 管理
- `config/`：配置管理
- `doctor/`：诊断工具
- `help/`：帮助系统

### 2.2.4 工具模块 (tools/)

**职责**：实现 AI 可调用的各种工具
为 Claude 提供文件操作、命令执行等能力。

**工具目录结构**：
- `BashTool/`：Shell 命令执行
- `FileReadTool/`：文件读取
- `FileEditTool/`：文件编辑
- `FileWriteTool/`：文件写入
- `GlobTool/`：文件模式匹配
- `GrepTool/`：内容搜索
- `WebSearchTool/`：网络搜索
- `WebFetchTool/`：网页获取
- `AgentTool/`：代理工具（多代理协作）
- `MCPTool/`：MCP 工具调用
- `SkillTool/`：技能调用
- `TodoWriteTool/`：待办事项
- `TaskCreateTool/` 等：任务管理

### 2.2.5 服务模块 (services/)

**职责**：提供核心业务服务
封装 API 调用、MCP 管理等功能。

**主要子目录**：
- `api/`：API 客户端
  - `claude.ts`：Claude API 封装
  - `bootstrap.ts`：启动数据获取
  - `filesApi.ts`：文件 API
- `mcp/`：MCP 服务管理
- `analytics/`：分析服务
- `compact/`：压缩服务
- `settingsSync/`：设置同步
- `lsp/`：LSP 服务
- `plugins/`：插件服务

### 2.2.6 桥接模块 (bridge/)

**职责**：处理远程会话和 IDE 集成
支持 WebSocket 和 HTTP 两种传输方式。

**核心文件**：
- `replBridge.ts`：REPL 桥接核心实现
- `bridgeApi.ts`：桥接 API 客户端
- `bridgeMessaging.ts`：消息处理
- `sessionRunner.ts`：会话运行器
- `remoteBridgeCore.ts`：远程桥接核心

### 2.2.7 UI 组件模块 (components/)

**职责**：提供 React UI 组件
基于 Ink 框架构建终端 UI。

**主要组件**：
- `Messages.tsx`：消息列表显示
- `TextInput.tsx`：文本输入
- `StatusLine.tsx`：状态栏
- `Spinner.tsx`：加载动画
- `Markdown.tsx`：Markdown 渲染
- `FullscreenLayout.tsx`：全屏布局
- `VirtualMessageList.tsx`：虚拟消息列表

### 2.2.8 终端渲染模块 (ink/)

**职责**：提供终端 UI 渲染能力
这是一个自建的 React 终端渲染框架。

**核心功能**：
- React 到 ANSI 转换
- 终端布局计算
- 键盘事件处理
- 颜色和样式管理

**子目录**：
- `components/`：UI 组件
- `hooks/`：React Hooks
- `layout/`：布局算法
- `events/`：事件处理

### 2.2.9 辅助模块

| 模块 | 职责 |
|------|------|
| `utils/` | 通用工具函数（200+ 文件） |
| `constants/` | 常量定义 |
| `types/` | TypeScript 类型定义 |
| `schemas/` | JSON Schema 定义 |
| `hooks/` | 自定义 React Hooks |
| `keybindings/` | 快捷键绑定 |
| `memdir/` | 内存存储 |
| `migrations/` | 数据迁移 |
| `vim/` | Vim 模式支持 |
| `voice/` | 语音功能 |

## 2.3 模块间依赖关系

### 2.3.1 依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│                      main.tsx                          │
│                   (应用入口)                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 entrypoints/cli.tsx                       │
│                   (CLI 入口)                           │
└─────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼                               ▼
┌───────────────────┐     ┌───────────────────┐
│  bootstrap/state  │     │   commands/*    │
│   (状态管理)     │     │   (命令系统)      │
└───────────────────┘     └───────────────────┘
            │                       │
            ▼                       ▼
┌───────────────────────────────────────────────────────────┐
│                      screens/REPL.tsx                    │
│                    (主屏幕)                              │
└───────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼                               ▼
┌───────────────────┐     ┌───────────────────┐
│  components/*   │     │    services/*     │
│   (UI 组件)       │     │   (业务服务)       │
└───────────────────┘     └───────────────────┘
            │                       │
            ▼                       ▼
┌───────────────────┐     ┌───────────────────┐
│     ink/*        │     │    tools/*       │
│  (终端渲染)      │     │   (工具实现)       │
└───────────────────┘     └───────────────────┘
                            │
                            ▼
                ┌───────────────────┐
                │     utils/*      │
                │   (工具函数)      │
                └───────────────────┘
```

### 2.3.2 核心依赖说明

**启动流程依赖链**：
1. `main.tsx` -> `entrypoints/cli.tsx` -> `bootstrap/state.ts`
2. 状态初始化后加载 `services/` 和 `commands/`
3. UI 渲染依赖 `components/` 和 `ink/`

**工具调用依赖链**：
1. `screens/REPL.tsx` -> `tools/` -> `services/api/`
2. 工具执行可能调用 `bridge/` 进行远程操作

**状态管理依赖**：
1. 所有组件依赖 `state/AppStateStore.ts`
2. 状态变更通过 `state/onChangeAppState.ts` 处理

## 2.4 代码组织规范

### 2.4.1 文件命名规范

| 类型 | 规范 | 示例 |
|------|------|-------|
| 组件文件 | PascalCase.tsx | `Messages.tsx`, `TextInput.tsx` |
| 工具文件 | PascalCase.ts | `BashTool.ts`, `FileEditTool.ts` |
| 命令目录 | kebab-case/ | `commands/commit/`, `commands/review/` |
| Hook 文件 | camelCase.ts | `useTextInput.ts`, `useGlobalKeybindings.tsx` |
| 类型文件 | camelCase.ts | `types/message.ts`, `types/ids.ts` |
| 常量文件 | camelCase.ts | `constants/prompts.ts`, `constants/oauth.ts` |

### 2.4.2 模块导出规范

```typescript
// 工具模块导出示例
export const BashTool: Tool = {
  name: 'Bash',
  // ...
}

// 命令模块导出示例
export default {
  name: 'commit',
  description: 'Create a git commit',
  // ...
}

// 组件导出示例
export function Messages(props: MessagesProps): JSX.Element {
  // ...
}
```

### 2.4.3 目录结构规范

每个功能模块遵循以下结构：
```
module/
├── index.ts          # 模块入口
├── types.ts          # 类型定义
├── utils.ts          # 工具函数
├── core.ts           # 核心逻辑
└── __tests__/        # 测试文件
```

### 2.4.4 代码分层原则

1. **表现层** (`components/`, `screens/`)
   - UI 组件和屏幕
   - 依赖状态层和服务层

2. **业务层** (`services/`, `commands/`, `tools/`)
   - 业务逻辑实现
   - 依赖工具层

3. **基础层** (`utils/`, `constants/`, `types/`)
   - 工具函数和类型定义
   - 不依赖其他层

4. **渲染层** (`ink/`)
   - 终端渲染引擎
   - 独立于业务逻辑

### 2.4.5 导入规范

```typescript
// 推荐的导入顺序
import { feature } from 'bun:bundle'           // 1. 外部框架
import type { SomeType } from 'some-lib'        // 2. 外部类型
import { localUtil } from './utils.js'          // 3. 本地工具
import { LocalComponent } from './Component.js' // 4. 本地组件
import type { LocalType } from './types.js'     // 5. 本地类型
```

### 2.4.6 特性开关

项目使用 `feature()` 函数进行条件编译和死代码消除：

```typescript
// 条件导入示例
const SleepTool = feature('PROACTIVE')
  ? require('./tools/SleepTool/SleepTool.js').SleepTool
  : null
```

这允许根据编译配置包含或排除特定功能。

---

## 小结

本章详细介绍了 Claude Code 的项目结构和模块划分。项目采用清晰的分层架构：

1. **入口层**：处理应用启动和命令行解析
2. **状态层**：管理全局应用状态
3. **命令层**：实现各种 CLI 命令
4. **工具层**：实现 AI 可调用的各种工具
5. **服务层**：提供核心业务服务
6. **UI 层**：提供终端用户界面
7. **渲染层**：提供终端渲染能力

这种模块化设计使得代码易于维护、测试和扩展。每个模块职责明确，依赖关系清晰。
